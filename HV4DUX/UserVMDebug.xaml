<UserControl
    x:Class="HV4DUX.UserVMDebug"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HV4DUX"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="UserVMDebugLoaded">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///HV4DUXTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border 
        BorderBrush="{ThemeResource HV4DButtonBorderStandard}"
        BorderThickness="1"
        HorizontalAlignment="Stretch"
        Height="320">

        <StackPanel Padding="5,5,5,5">

            <local:UserFilterNew x:Name="FilterByUUID" Label="{x:Bind LabelHVID}" Value="{x:Bind ValueHVID}"/>

            <local:UserFilterTime x:Name="FilterByTime"/>

            <local:UserFilterType x:Name="FilterByType" Label="{x:Bind LabelType}" Value="{x:Bind ValueType}"/>

        </StackPanel>

    </Border>

</UserControl>
